<%= form_with(model: group, local: true) do |form| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
        <% group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="form-group string optional">
      <%= form.label :title, :class => "string optional control-label" %>
      <%= form.text_field :title, :class => "string optional control-label" %>
    </div>

    <div class="form-group">
      <%= form.label :description, :class => "string optional control-label" %>
      <%= form.text_area :description, :class => "string optinaol control-label" %>
    </div>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
